# CONFIG FILE: job_scheduler.config
# Customize job_scheduler behavior

# 🔹 Where to store jobs (default: ~/job_logs)
JOB_DIR=$HOME/job_logs

# 🔹 Where to archive jobs (default: JOB_DIR/archive)
ARCHIVE_DIR=$JOB_DIR/archive

# 🔹 How many jobs before auto-archiving? (default: 100)
ARCHIVE_THRESHOLD=100

# 🔹 Log filename format (default: job_XXX.log where XXX is the job index)
LOG_FILE_NAME="job_XXX.log"

# 🔹 Watch refresh interval (default: 3 seconds)
WATCH_REFRESH_INTERVAL=3

