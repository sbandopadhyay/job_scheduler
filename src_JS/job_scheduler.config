# CONFIG FILE: job_scheduler.config
# Customize job_scheduler behavior

# 🔹 Directory for job logs (default: ~/job_logs)
JOB_DIR=$HOME/job_logs

# 🔹 Directory for queued jobs (default: JOB_DIR/queue)
QUEUE_DIR=$JOB_DIR/queue

# 🔹 Directory for archived jobs (default: JOB_DIR/archive)
ARCHIVE_DIR=$JOB_DIR/archive

# 🔹 Maximum number of jobs that can run concurrently (default: 4, set to 0 for unlimited)
MAX_CONCURRENT_JOBS=4

# 🔹 Number of completed jobs before auto-archiving (default: 100, set to 0 to disable)
ARCHIVE_THRESHOLD=100

# 🔹 Log filename template (XXX replaced with zero-padded job index, e.g., job_001.log)
LOG_FILE_NAME="job_XXX.log"

# 🔹 Refresh interval for -watch command in seconds (default: 3)
WATCH_REFRESH_INTERVAL=3